@{
    Layout = "~/Views/Shared/_Layout.GovUK.Sections.cshtml";
}

<div class="grid-row">
    <div class="column-third">
        @Html.Action("ImportNavigation", "Menu", new { area = "Admin", section = ImportNavigationSection.Notification })
    </div>

    <div class="column-two-thirds">
        @RenderBody()
    </div>
</div>

@section top_content_box {
    @Html.Action("NotificationSwitcher", "Home")
}

@section head {
    @RenderSection("head", required: false)
}

@section scripts {
    @RenderSection("scripts", required: false)
}