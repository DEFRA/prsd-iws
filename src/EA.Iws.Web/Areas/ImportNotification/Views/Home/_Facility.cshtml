@model EA.Iws.Core.ImportNotification.Summary.InProgressImportNotificationSummary

<div class="overview-block">
    @if (Model.Facilities != null && Model.Facilities.Count > 0)
    {
        for (int i = 0; i < Model.Facilities.Count; i++)
        {
            <div class="overview-block-element">
                @if (!string.IsNullOrWhiteSpace(Model.Facilities[i].Name))
                {
                    if (Model.Facilities[i].IsActualSite)
                    {
                        <text>Actual site of recovery</text>
                        <br/><br />
                    }
                    
                    @Html.DisplayFor(m => m.Facilities[i].Name)
                    <br />
                }

                @if (!Model.Facilities[i].Address.IsEmpty())
                {
                    @Html.DisplayFor(m => m.Facilities[i].Address.AddressLine1)<text>, </text>
                    @Html.DisplayFor(m => m.Facilities[i].Address.TownOrCity)<text> </text>
                    @Html.DisplayFor(m => m.Facilities[i].Address.PostalCode)<text>, </text>
                    @Html.DisplayFor(m => m.Facilities[i].Address.Country)
                }

            </div>

            if (!Model.Facilities[i].Contact.IsEmpty())
            {
                <div class="overview-block-element">
                    @Html.DisplayFor(m => m.Facilities[i].Contact)
                </div>
            }

            if (i < (Model.Facilities.Count - 1))
            {
                <hr />
            }
        }
    }
    else
    {
        <div class="overview-block-element"> @ViewBag.SectionIncompleteText</div>
    }
</div>