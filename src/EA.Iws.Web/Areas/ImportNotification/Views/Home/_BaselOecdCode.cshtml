@model EA.Iws.Core.ImportNotification.Summary.InProgressImportNotificationSummary

<details>
    <summary class="block">
        Basel or OECD code @Html.ActionLink("Change", "Index", "WasteType", null, new { @class = "right-float" })
    </summary>
    <div class="expanded-details">
        <div class="panel-indent">
            @if (!Model.WasteType.BaselCode.IsEmpty())
            {
                if (Model.WasteType.BaselCode.IsNotApplicable)
                {
                    <p>Not applicable</p>
                }
                else
                {
                    @Html.Partial("_CodeDefinitionList", Model.WasteType.BaselCode.WasteCodes)
                }
            }
            else
            {
                <p>@ViewBag.SectionIncompleteText</p>
            }
        </div>
    </div>
</details>