@using EnumHelper = EA.Prsd.Core.Helpers.EnumHelper
@model EA.Iws.Core.ImportNotification.Summary.InProgressImportNotificationSummary
@using Resource = EA.Iws.Web.Areas.ImportNotification.Views.Home.IndexResources

<details>
    <div class="overview-block">
        @if (Model.IntendedShipment != null && !Model.IntendedShipment.IsEmpty())
        {
            <div class="overview-block-element">
                <dl>
                    <dt>@Resource.ShipmentNumberTitle</dt>
                    @if (Model.IntendedShipment.TotalShipments.HasValue)
                    {
                        <dd>@Model.IntendedShipment.TotalShipments</dd>
                    }
                    else
                    {
                        <dd>@Resource.ShipmentNotEnteredText</dd>
                    }

                    <dt>@Resource.ShipmentQuantityTitle</dt>
                    @if (Model.IntendedShipment.Quantity.HasValue && Model.IntendedShipment.Units.HasValue)
                    {
                        <dd>@Html.Raw(string.Format("{0:0.####} {1}", Model.IntendedShipment.Quantity, EnumHelper.GetShortName(Model.IntendedShipment.Units)))</dd>
                    }
                    else
                    {
                        <dd>@Resource.ShipmentNotEnteredText</dd>
                    }

                    <dt>@Resource.ShipmentDatesTitle</dt>
                    @if (Model.IntendedShipment.Start.HasValue && Model.IntendedShipment.End.HasValue)
                    {
                        <dd>@Html.Gds().DisplayDateRangeFor(m => m.IntendedShipment.Start.Value, m => m.IntendedShipment.End.Value)</dd>
                    }
                    else
                    {
                        <dd>@Resource.ShipmentNotEnteredText</dd>
                    }
                </dl>
            </div>
        }
        else
        {
            <div class="overview-block-element">@ViewBag.SectionIncompleteText</div>
        }
    </div>
</details>