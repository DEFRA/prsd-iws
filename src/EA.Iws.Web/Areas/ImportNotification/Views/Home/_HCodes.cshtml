@model EA.Iws.Core.ImportNotification.Summary.InProgressImportNotificationSummary

<details>
    <summary class="block">
        H or HP Codes @Html.ActionLink("Change", "Index", "WasteType", null, new { @class = "right-float" })
    </summary>
    <div class="expanded-details">
        <div class="panel-indent">
            @if (!Model.WasteType.HCodes.IsEmpty())
            {
                if (Model.WasteType.HCodes.IsNotApplicable)
                {
                    <p>Not applicable</p>
                }
                else
                {
                    @Html.Partial("_CodeDefinitionList", Model.WasteType.HCodes.WasteCodes)
                }
            }
            else
            {
                <p>@ViewBag.SectionIncompleteText</p>
            }
        </div>
    </div>
</details>

