@using EA.Iws.Web.Views.NewNotification
@using EA.Iws.Core.Notification
@using EnumHelper = EA.Prsd.Core.Helpers.EnumHelper
@model EA.Iws.Web.ViewModels.NewNotification.CompetentAuthorityChoiceViewModel
@{
    ViewBag.Title = CompetentAuthorityResources.Title;
}

<header class="hgroup text">
    <h1 class="heading-large">@CompetentAuthorityResources.Header</h1>
    <p>@CompetentAuthorityResources.HeaderDescription</p>
</header>

@using (Html.BeginForm())
{
    @Html.Gds().ValidationSummary()
    @Html.AntiForgeryToken()

    <div class="form-group @Html.Gds().FormGroupClass(m => m.AuthorityType)">
        <fieldset class="govuk-fieldset">
            <legend class="form-label">
                @CompetentAuthorityResources.Title
            </legend>

            @Html.Gds().ValidationMessageFor(m => m.AuthorityType)

            <div class="multiple-choice">
                @Html.RadioButtonFor(m => m.AuthorityType, "England", new { @class = "", @id = "England" })
                <label class="" for="England">
                    @EnumHelper.GetDisplayName(UKCompetentAuthority.England)
                </label>
            </div>

            <div class="multiple-choice">
                @Html.RadioButtonFor(m => m.AuthorityType, "Scotland", new { @class = "", @id = "Scotland" })
                <label class="" for="Scotland">
                    @EnumHelper.GetDisplayName(UKCompetentAuthority.Scotland)
                </label>
            </div>

            <div class="multiple-choice">
                @Html.RadioButtonFor(m => m.AuthorityType, "NorthernIreland", new { @class = "", @id = "NorthernIreland" })
                <label class="" for="NorthernIreland">
                    @EnumHelper.GetDisplayName(UKCompetentAuthority.NorthernIreland)
                </label>
            </div>

            <div class="multiple-choice">
                @Html.RadioButtonFor(m => m.AuthorityType, "Wales", new { @class = "", @id = "Wales" })
                <label class="" for="Wales">
                    @EnumHelper.GetDisplayName(UKCompetentAuthority.Wales)
                </label>
            </div>

        </fieldset>
    </div>
    <div class="form-group">
        <button class="button" type="submit">@Constants.ContinueButtonText</button>
    </div>
}