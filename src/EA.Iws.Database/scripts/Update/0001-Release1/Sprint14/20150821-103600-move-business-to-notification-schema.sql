ALTER SCHEMA Notification 
    TRANSFER Business.Carrier;

ALTER SCHEMA Notification 
    TRANSFER Business.Exporter;

ALTER SCHEMA Notification 
    TRANSFER Business.Facility;

ALTER SCHEMA Notification 
    TRANSFER Business.Importer;

ALTER SCHEMA Notification 
    TRANSFER Business.OperationCodes;

ALTER SCHEMA Notification 
    TRANSFER Business.Organisation;

ALTER SCHEMA Notification 
    TRANSFER Business.PackagingInfo;

ALTER SCHEMA Notification 
    TRANSFER Business.PhysicalCharacteristicsInfo;

ALTER SCHEMA Notification 
    TRANSFER Business.Producer;

ALTER SCHEMA Notification 
    TRANSFER Business.RecoveryInfo;

ALTER SCHEMA Notification 
    TRANSFER Business.ShipmentInfo;

ALTER SCHEMA Notification 
    TRANSFER Business.WasteAdditionalInformation;

ALTER SCHEMA Notification 
    TRANSFER Business.WasteCodeInfo;

ALTER SCHEMA Notification 
    TRANSFER Business.WasteComposition;

ALTER SCHEMA Notification 
    TRANSFER Business.WasteType;